package com.htmake.htbot.domain.shop.repository;

import com.htmake.htbot.domain.shop.entity.RandomShopWeapon;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RandomShopWeaponRepository extends JpaRepository<RandomShopWeapon, String> {
}
